import styled from 'styled-components';
import tw from 'twin.macro';

const CreateDiv = styled.div`
    ${tw`
    flex
    px-2
    gap-3
    justify-center
    content-center
    items-center
    h-9
    cursor-default
  `}
`;

export { CreateDiv };
