import styled from 'styled-components';
import tw from 'twin.macro';

const Container = styled.div`
    ${tw`
    bg-default-warmgray
    `}
`;

const MarginWrapper = styled.div`
    ${tw`
  ml-[80px]
`}
`;

export { Container, MarginWrapper };
